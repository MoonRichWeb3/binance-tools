# 05 AI 模块

本文记录 `binance-tools` 当前 AI 模块的设计、配置、桌面 UI 和请求流程。实现参考 Zed Agent 的交互方式，但按本项目需要做了轻量化实现。

## 代码位置

| 模块 | 路径 | 说明 |
| --- | --- | --- |
| 核心 AI 配置和请求 | `src/ai/mod.rs` | 读取 `config/ai.json`、选择 provider/model、发送普通和流式请求 |
| AI Chat 面板 | `examples/desktop-gpui/src/ui/ai/chat.rs` | 右侧 Agent 面板、模型选择、消息渲染、流式追加 |
| AI Provider 设置 | `examples/desktop-gpui/src/ui/ai/providers.rs` | Settings 页面、Provider 新增/编辑/删除 |
| Dashboard 集成 | `examples/desktop-gpui/src/ui/dashboard.rs` | 右侧 AI 面板、Settings 最大化和市场分析事件接入 |
| 标题栏入口 | `examples/desktop-gpui/src/ui/title_bar.rs` | 右侧 `AI` 按钮 |

## 配置文件

默认配置文件：

```text
config/ai.json
```

顶层结构：

| 字段 | 说明 |
| --- | --- |
| `agent` | Agent 行为配置，包括默认模型、输入框行数、参数规则等 |
| `language_models` | 内置 provider 和用户新增 OpenAI-compatible provider |
| `language_models.openai_compatible` | 用户在 Settings 里保存的 provider |

Provider 的主要字段：

| 字段 | 说明 |
| --- | --- |
| `api_url` | OpenAI-compatible base URL，例如 `https://api.example.com/v1` |
| `api_key` | 兼容旧配置；加载后会迁移到 SQLite，不再建议直接写入 |
| `api_key_env` | 内置 provider 的环境变量回退配置；模型选择 UI 不把它视为已配置 key |
| `api_format` | 当前支持 `open_ai_chat`、`anthropic`、`google` |
| `available_models` | Provider 下可选模型列表 |

OpenAI-compatible URL 会自动拼接 `/chat/completions`。如果用户直接填完整 `/chat/completions` endpoint，代码会识别并避免重复拼接。

真实 API Key 保存到 SQLite `ai_provider_keys` 表。`config/ai.json` 只保存 provider、URL、模型列表和参数配置，不保存真实密钥。

## Provider 选择规则

请求时使用当前 `ModelSelection { provider, model }` 查找 provider。

查找逻辑：

1. 优先在 `language_models.openai_compatible` 中按 provider 名查找。
2. 如果自定义 provider 下的 `available_models` 包含当前 model，则使用这个自定义 provider。
3. 如果自定义 provider 不匹配当前 model，再回退到内置 provider。

这样可以避免自定义 provider 和内置 provider 同名时，用错 URL 或 API Key。

建议：自定义 provider 避免命名为 `deepseek`、`openai` 这类内置名字，优先使用 `sensenova`、`z-ai`、`deepseek-ai` 这种明确名称。

## Settings 页面

AI Settings 页面包含：

- `External Agents`：展示 Codex CLI 占位信息。
- `Model Context Protocol (MCP) Servers`：MCP 占位区域。
- `LLM Providers`：展示用户保存的 OpenAI-compatible provider。

功能清单：

- [✓] 【完成】 `Configure` 从模型下拉进入 Settings。
- [✓] 【完成】 顶部返回按钮关闭 Settings。
- [✓] 【完成】 最大化按钮切换 Settings 是否覆盖主内容区。
- [✓] 【完成】 `Add Provider` 打开新增弹窗。
- [✓] 【完成】 展开 provider 后显示 API URL、Credential、模型列表、配置文件位置和 Key Store。
- [✓] 【完成】 展开 provider 后可点击 `Edit`、`Clear Key` 和 `Delete`。
- [✓] 【完成】 保存 provider 后，只有本地 DB key 存在且非空时列表显示绿色勾。
- [✓] 【完成】 `Clear Key` 清空 SQLite key，并把 provider 标记为未配置。
- [✓] 【完成】 `Delete` 删除 provider 配置，并同步删除 SQLite 中的 key。
- [✓] 【完成】 新增/编辑时校验 Provider Name、API URL、API Key、Model Name 不能为空。
- [✓] 【完成】 新增时 Provider Name 不能和现有 provider 重复。
- [✓] 【完成】 API Key 编辑时会去掉默认填充的长串 `0000`。
- [✓] 【完成】 默认没有配置 key 时，模型选择列表不展示该 provider 的模型。

Provider 弹窗字段：

| 字段 | 说明 |
| --- | --- |
| Provider Name | provider key |
| API URL | OpenAI-compatible base URL |
| API Key | API Key |
| Model Name | 模型名，必须和服务商支持的模型名一致 |
| Max Completion Tokens | completion token 上限 |
| Max Output Tokens | 输出 token 上限 |
| Max Tokens | 上下文 token 上限 |
| Supports tools/images/parallel_tool_calls/prompt_cache_key | 能力声明，主要用于 UI 和后续扩展 |

## Chat 面板

右侧 AI Chat 面板当前能力：

- [✓] 【完成】 标题栏右侧 `AI` 按钮打开/关闭。
- [✓] 【完成】 右上角菜单只保留 `Rules`、`Profiles`、`Settings`、`Toggle Threads Sidebar` 和 `Reauthenticate`。
- [✓] 【完成】 `Settings` 菜单项进入 AI Provider Settings 页面。
- [✓] 【完成】 空线程显示底部输入区和工具栏。
- [✓] 【完成】 有消息后，上方为 thread，下方为固定 composer。
- [✓] 【完成】 composer 默认占约 `1/5` 高度，展开后占约 `4/5` 高度。
- [✓] 【完成】 Enter 发送消息。
- [✓] 【完成】 发送中按钮显示红色 spinner；完成或停止后恢复普通箭头状态。
- [✓] 【完成】 发送后保留底部模型选择器和 toolbar。
- [✓] 【完成】 模型下拉只展示已配置 DB key 的 provider 模型。
- [✓] 【完成】 模型下拉使用 Zed 风格列表，当前选中模型显示勾选。
- [✓] 【完成】 `Add model` / `Configure` 入口固定在模型下拉底部。
- [✓] 【完成】 Context 图标 hover 显示输入/输出 token 估算。
- [✓] 【完成】 Assistant 回复支持 Markdown 渲染和文本选择复制。
- [✓] 【完成】 回复下方提供点赞、点踩、复制、重试、继续按钮。
- [✓] 【完成】 API 错误以浅红色卡片固定显示在输入框上方。
- [✓] 【完成】 多条错误只保留最新一条，关闭错误会清理失败状态。

点赞/点踩当前只是本地 UI 状态，不会反馈给模型或服务端。

## 历史会话

历史会话参考 Zed 的 threads sidebar：

- [✓] 【完成】 点击 `Toggle Threads Sidebar` 后显示左侧历史会话列表。
- [✓] 【完成】 打开历史会话列表时，窗口向右扩宽，避免压缩当前聊天区域。
- [✓] 【完成】 历史列表支持搜索。
- [✓] 【完成】 点击历史会话后恢复消息内容和选中模型。
- [✓] 【完成】 历史会话可删除。
- [✓] 【完成】 会话数据保存到 SQLite `ai_chat_threads` 表。
- [✓] 【完成】 会话正文使用 `zstd` 压缩保存。
- [✓] 【完成】 读取时兼容旧的 `json` 明文数据。
- [✓] 【完成】 页面触发的 AI 分析会保存当次规则快照，规则后续修改后历史会话仍保留旧规则。

## 页面规则

- [✓] 【完成】 AI 分析规则保存到 SQLite `ai_rules` 表，内容使用 `zstd` 压缩。
- [✓] 【完成】 每个页面或功能使用独立 `context_key`，同一个 AI 面板里新开分析也会按页面上下文读取独立规则。
- [✓] 【完成】 默认规则只在数据库缺失时初始化，用户修改后的规则不会被程序启动覆盖。
- [✓] 【完成】 发送给模型的真实 prompt 会追加当前页面规则；聊天界面仍只展示摘要。
- [✓] 【完成】 历史会话保存规则快照，重试旧消息时沿用当时保存的真实请求内容。
- [✓] 【完成】 `Rules` 菜单打开规则管理页面，左侧支持新增、搜索、内置规则分组和规则列表。
- [✓] 【完成】 规则详情页支持编辑名称、`context_key`、规则正文、格式 `Text` / `Markdown`、启用/禁用、保存、复制、刷新和撤销未保存正文。
- [✓] 【完成】 Markdown 规则支持点击眼睛图标打开预览弹窗，预览使用和 AI Chat 回复一致的 Markdown 渲染器，保存时仍写入原始 Markdown 文本。
- [✓] 【完成】 `Rules` 菜单打开独立弹窗，不挤占右侧 AI Chat 面板。

## 流式输出

OpenAI-compatible provider 使用流式输出：

```json
{
  "stream": true
}
```

请求入口：

```rust
send_chat_with_model_streaming_blocking(settings, selection, messages, on_delta)
```

实现方式：

1. UI 发送请求后先插入一条 `Thinking...` assistant 占位消息。
2. 后台线程请求 `/chat/completions` 并读取 SSE。
3. 每读到一段 `choices[0].delta.content`，通过 channel 发送给 UI。
4. UI 每隔约 `24ms` 轮询 delta，并追加到最后一条 assistant 消息。
5. 收到 `[DONE]` 后结束 loading。
6. 如果流式中途失败，失败状态保存在最新 assistant 消息中，UI 在输入框上方显示错误卡片。

Anthropic 和 Google 当前仍走非流式回退：完整响应返回后一次性显示。

## 错误处理

AI HTTP 错误会统一转成中文提示：

| 状态 | 提示方向 |
| --- | --- |
| `401` | API Key 未授权、过期或环境变量未生效 |
| `403` | 权限、余额、区域或模型访问被拒绝 |
| `404` | 模型名或 API URL 不匹配 |
| `429` | 频率、额度或服务商限流 |
| `5xx` | 服务商临时错误，可重试 |
| 网络超时/连接失败 | 检查网络、代理、DNS 或本地服务 |

HTTP 客户端配置：

- connect timeout：`30s`
- request timeout：`300s`
- `reqwest` 使用 `rustls-tls-native-roots` 和 `system-proxy`，尽量贴近系统网络配置。

## Context 用量

底部 Context 图标显示本地估算值：

```text
Input: 7.7k / 168k
Output: 23 / 32k
```

说明：

- Input used：当前会话消息 + 输入框内容的本地估算。
- Input limit：当前模型 `max_tokens`。
- Output used：最近一条 assistant 回复的本地估算。
- Output limit：优先使用 `max_output_tokens`，没有时使用 `max_completion_tokens`。
- 当前不是官方 tokenizer，仅用于 UI 参考。

## 已知边界

- x 【未完成】 工具调用、Web 搜索、MCP 目前仍是 UI/配置占位，没有真实执行链路。
- x 【未完成】 Anthropic/Google 尚未接入原生流式协议，目前通过非流式响应回填。
- [✓] 【完成】 Like/Dislike 已随会话消息保存到 `ai_chat_threads`。
- x 【未完成】 多模型批量添加尚未做完整 UI。
